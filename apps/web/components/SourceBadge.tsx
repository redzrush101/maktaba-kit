export function SourceBadge({ source }: { source: string }) {
  return <span className="rounded-full border border-line bg-paper px-2 py-0.5 font-sans text-[10px] uppercase tracking-wide text-muted">{source}</span>;
}
